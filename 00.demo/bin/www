#! /usr/bin/env node
const importLocal = require('import-local');
const npmlog = require('npmlog');
if (importLocal(__filename)) {
  npmlog.info('ppk-cli', 'Using local version of ppk-cli');
} else {
  // npmlog.info('ppk-cli', 'global command');
  require('../lib/core');
}