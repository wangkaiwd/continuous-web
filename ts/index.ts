// const echo = <T> (arg: T): T => {
//   return arg;
// };
//
// // 函数无法进行类型推导
// const result = echo(1);

// const swap = <T, U> (arr: [T, U]): [U, T] => {
//   return [arr[1], arr[0]];
// };
//
// const result = swap(['a', 1]);
